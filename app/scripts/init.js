//let lazyLoadInstance = new LazyLoad();
//new WOW().init();
app.burger.init();
app.dropMenu.init();
/* document.querySelector('.page-up').addEventListener('click', () => {
	window.scrollTo({
		top: 0,
		behavior: "smooth",
	});
}); */
